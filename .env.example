# No API key needed — all models run locally
# Models are downloaded automatically on first run:
#   facebook/bart-large-cnn  (~1.6 GB) — summarization
#   google/flan-t5-base      (~250 MB) — chatbot Q&A
#   d4data/biomedical-ner-all            — NER
